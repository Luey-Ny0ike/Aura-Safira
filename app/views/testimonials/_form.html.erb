<%= form_with(model: testimonial, local: true) do |form| %>
  <% if testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>

      <ul>
      <% testimonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :testimonial_name %>
  </div>
  <div class="field">
    <%= form.label :position %>
    <%= form.text_field :position, id: :testimonial_position %>
  </div>
  <div class="field">
    <%= form.label :testimony %>
    <%= form.text_field :testimony, id: :testimonial_testimony %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
